/**
 * Mock-data barrel. Imported by `design-system/providers/_mock-threads.ts`
 * (and any future feature mock module). Compositions never import directly.
 */
export { MOCK_AGENTS, MOCK_ENROLLMENTS } from './agents';
export { MOCK_MESSAGES, MOCK_PARTICIPANTS } from './messages';
export { MOCK_ORG_SLUG, MOCK_ORGS } from './orgs';
export { MOCK_THREADS } from './threads';
