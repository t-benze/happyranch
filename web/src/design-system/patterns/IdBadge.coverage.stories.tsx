import type { Meta, StoryObj } from '@storybook/react';
import { IdBadge } from './IdBadge';

const meta = { title: 'Design System/Coverage/IdBadge', component: IdBadge, tags: ['autodocs'] } satisfies Meta<typeof IdBadge>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
