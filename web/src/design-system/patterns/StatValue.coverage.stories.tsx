import type { Meta, StoryObj } from '@storybook/react';
import { StatValue } from './StatValue';

const meta = { title: 'Design System/Coverage/StatValue', component: StatValue, tags: ['autodocs'] } satisfies Meta<typeof StatValue>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
