import type { Meta, StoryObj } from '@storybook/react';
import Mermaid from './Mermaid';

const meta = { title: 'Design System/Coverage/Mermaid', component: Mermaid, tags: ['autodocs'] } satisfies Meta<typeof Mermaid>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
