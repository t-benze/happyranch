import type { Meta, StoryObj } from '@storybook/react';
import { FormField } from './FormField';

const meta = { title: 'Design System/Coverage/FormField', component: FormField, tags: ['autodocs'] } satisfies Meta<typeof FormField>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
