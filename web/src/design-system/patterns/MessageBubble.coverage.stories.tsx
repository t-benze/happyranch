import type { Meta, StoryObj } from '@storybook/react';
import { MessageBubble } from './MessageBubble';

const meta = { title: 'Design System/Coverage/MessageBubble', component: MessageBubble, tags: ['autodocs'] } satisfies Meta<typeof MessageBubble>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
