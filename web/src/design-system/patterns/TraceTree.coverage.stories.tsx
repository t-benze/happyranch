import type { Meta, StoryObj } from '@storybook/react';
import { TraceTree } from './TraceTree';

const meta = { title: 'Design System/Coverage/TraceTree', component: TraceTree, tags: ['autodocs'] } satisfies Meta<typeof TraceTree>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
