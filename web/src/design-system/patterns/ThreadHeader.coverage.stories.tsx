import type { Meta, StoryObj } from '@storybook/react';
import { ThreadHeader } from './ThreadHeader';

const meta = { title: 'Design System/Coverage/ThreadHeader', component: ThreadHeader, tags: ['autodocs'] } satisfies Meta<typeof ThreadHeader>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
