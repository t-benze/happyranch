import type { Meta, StoryObj } from '@storybook/react';
import { FilterSidebar } from './FilterSidebar';

const meta = { title: 'Design System/Coverage/FilterSidebar', component: FilterSidebar, tags: ['autodocs'] } satisfies Meta<typeof FilterSidebar>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
