import type { Meta, StoryObj } from '@storybook/react';
import { ContentWrap } from './ContentWrap';

const meta = { title: 'Design System/Coverage/ContentWrap', component: ContentWrap, tags: ['autodocs'] } satisfies Meta<typeof ContentWrap>;
export default meta;
type Story = StoryObj;
export const Coverage: Story = {};
