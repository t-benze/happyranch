/**
 * Re-export from the shared module so existing feature imports
 * continue to resolve without cross-feature boundary violations.
 */
export { NewThreadDialog } from '@/shared/threads/NewThreadDialog';
