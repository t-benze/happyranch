"""Per-family `happyranch` CLI command modules (split from cli/main.py).

Each module exposes its `cmd_*` handlers and a `register(sub)` that wires
that family's argparse subparsers onto the top-level subparsers action.
"""
